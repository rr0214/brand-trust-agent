# Core LLM & agent dependencies
openai>=1.30.0

# Vector store (local, no server needed)
chromadb>=0.5.0

# Arize AX instrumentation
arize-otel>=0.7.0
openinference-instrumentation-openai>=0.1.12
openinference-semantic-conventions>=0.1.9
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp>=1.24.0

# Evaluation (Arize Phoenix eval framework — used for dev workflow)
arize-phoenix[evals]>=4.0.0

# Frontend
streamlit>=1.35.0

# Utilities
python-dotenv>=1.0.0
pandas>=2.1.0
